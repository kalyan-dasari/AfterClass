{
  "name": "afterclass",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev:all",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm --prefix frontend run build && node scripts/sync-dist.mjs"
  }
}
